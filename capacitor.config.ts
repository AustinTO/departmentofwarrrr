import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.departmentofwarrr',
  appName: 'Department of WARRR',
  webDir: 'dist'
};

export default config;
